{
  "name": "crud-app",
  "version": "1.0.0",
  "description": "CRUD Application with React.js Frontend and Node.js Backend",
  "scripts": {
    "dev": "concurrently \"cd server && npm start\" \"cd server/client && npm run dev\"",
    "server": "cd server && npm start",
    "client": "cd server/client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.13.2"
  }
}
